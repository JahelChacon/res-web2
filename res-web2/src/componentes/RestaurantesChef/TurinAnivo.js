import React from "react";
import RestauranteTemplate from "../Compartidos/Restaurantes/RestauranteTemplate";

export default function TurinAnivo({ token }) {
    return (
        <RestauranteTemplate token={token} restaurante='Turin Anivo' barras={2}/>
    )
}
